import App from './App';
import React from 'react';
import { render } from "@testing-library/react";
import "@testing-library/jest-dom/extend-expect";

describe.skip("App", () => {
    test("Route component should load", () => {
        // TODO: Render app, check for text of RouteSelect child to make sure it rendered
        // render(<App />);
        // expect(getByText(/Select a Route/i)).toBeInTheDocument();
    })
})